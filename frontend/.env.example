# Copy this file to frontend/.env.local when the backend does not run on the
# local Flask default.

# Public frontend setting used by Next.js. Include protocol and port.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:5000/
